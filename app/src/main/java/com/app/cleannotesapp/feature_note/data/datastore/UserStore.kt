package com.app.cleannotesapp.feature_note.data.datastore

import android.content.Context
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.map

class UserStore(private val context: Context) {
//    companion object {
//        private val Context.dataStore: DataStore<Preferences> by preferencesDataStore("userToken")
//        private val LAST_SYNC_KEY = stringPreferencesKey("last_sync_time")
//    }
//
//    val getLastSyncTime: Flow<String> = context.dataStore.data.map { preferences ->
//        preferences[LAST_SYNC_KEY] ?: ""
//    }
//
//    suspend fun setLastSyncTime(time: String) {
//        context.dataStore.edit { preferences ->
//            preferences[LAST_SYNC_KEY] = time
//        }
//    }
}
